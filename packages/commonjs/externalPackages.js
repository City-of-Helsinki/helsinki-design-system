/**
 * List packages that should NOT be bundled to the complied js.
 * Listed packages are listed in the 'dependencies' of the generated package.json
 */
const dependencies = ['cookie'];
module.exports = dependencies;
