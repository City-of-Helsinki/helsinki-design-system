{
  "name": "hds-core",
  "version": "0.4.2-alpha.0",
  "description": "Core styles for the Helsinki Design System",
  "author": "Anssi Lehtonen <lehtovaaralainen@gmail.com>",
  "homepage": "https://github.com/City-of-Helsinki/helsinki-design-system#readme",
  "license": "MIT",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "rimraf lib/ && yarn run postcss  src/*.css src/**/*.css --base src -d lib/",
    "start": "yarn run postcss src/*.css src/**/*.css --base src -d lib/ --watch"
  },
  "devDependencies": {
    "copyfiles": "^2.1.1",
    "normalize.css": "^8.0.1",
    "postcss": "^7.0.18",
    "postcss-cli": "^6.1.3",
    "postcss-import": "^12.0.1",
    "postcss-inline-svg": "^4.1.0",
    "postcss-preset-env": "^6.7.0",
    "rimraf": "^3.0.0"
  }
}
