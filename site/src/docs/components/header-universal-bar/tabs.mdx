---
slug: '/components/header-universal-bar/tabs'
title: 'HeaderUniversalBar'
---

import { StatusLabel, Notification } from 'hds-react';

import LeadParagraph from '../../../components/LeadParagraph';
import Layout from '../../../components/layout';
import PageTabs from '../../../components/PageTabs';

# HeaderUniversalBar

<StatusLabel type="alert">WIP</StatusLabel>
<StatusLabel type="success" style={{ marginLeft: 'var(--spacing-xs)' }}>
  Accessible
</StatusLabel>

<LeadParagraph>A header universal bar provides useful quick links for the user of the service.</LeadParagraph>

<Notification label="A work in progress!" className="siteNotification">
  The header universal bar component is part of a collection of new navigation components that the HDS team is in the
  process of making. This means that this component is subject to change and we don't recommend using it in production
  just yet.
</Notification>

<PageTabs pageContext={props.pageContext}>
  <PageTabs.TabList>
    <PageTabs.Tab href="/">Usage</PageTabs.Tab>
    <PageTabs.Tab href="/code">Code</PageTabs.Tab>
  </PageTabs.TabList>
  <PageTabs.TabPanel>{props.children}</PageTabs.TabPanel>
</PageTabs>

export default ({ pageContext, children }) => <Layout pageContext={pageContext}>{children}</Layout>;
