---
slug: '/elements/components/buttons'
title: 'Button'
---

import { StatusLabel, Tabs } from 'hds-react';
import { navigate } from 'gatsby';

import Usage from './usage/usage.mdx';
import Code from './code/code.mdx';
import Accessibility from './accessibility/accessibility.mdx';
import Customisation from './customisation/customisation.mdx';
import LeadParagraph from '../../../../components/LeadParagraph';

# Button

<StatusLabel type="info">Stable</StatusLabel>
<StatusLabel type="success" style={{ marginLeft: 'var(--spacing-xs)' }}>
  Accessible
</StatusLabel>

<LeadParagraph>
  Buttons are meant to make actions easily visible and understandable to the user. HDS offers different kinds of button variations which each suit for different needs.
</LeadParagraph>


<Tabs initiallyActiveTab={() => props.initiallyActiveTab}>
  <Tabs.TabList className="pageTabsList">
    <Tabs.Tab onClick={() => navigate(`/elements/components/buttons/usage`)}>Usage</Tabs.Tab>
    <Tabs.Tab onClick={() => navigate(`/elements/components/buttons/code`)}>Code</Tabs.Tab>
    <Tabs.Tab onClick={() => navigate(`/elements/components/buttons/accessibility`)}>Accessibility</Tabs.Tab>
    <Tabs.Tab onClick={() => navigate(`/elements/components/buttons/customisation`)}>Customisation</Tabs.Tab>
  </Tabs.TabList>
  <Tabs.TabPanel>
    <Usage />
  </Tabs.TabPanel>
  <Tabs.TabPanel>
    <Code />
  </Tabs.TabPanel>
  <Tabs.TabPanel>
    <Accessibility />
  </Tabs.TabPanel>
  <Tabs.TabPanel>
    <Customisation />
  </Tabs.TabPanel>
</Tabs>

export default ({ children }) => <>{children}</>;
